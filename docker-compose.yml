version: '3.8'
services:
  n8n:
    image: n8nio/n8n:latest
    restart: always
    ports:
      - "5678:5678"
    volumes:
      - ./n8n_data:/home/node/.n8n
    environment:
      - N8N_ENCRYPTION_KEY=your_secret_key_here  # สำคัญมาก: ใช้คีย์เดียวกันทั้ง 2 เครื่อง
      - TZ=Asia/Bangkok